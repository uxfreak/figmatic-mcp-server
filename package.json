{
  "name": "figmatic",
  "version": "1.0.0",
  "description": "A bidirectional communication system that enables AI agents (like Claude Code) to execute Figma Plugin API commands remotely via WebSocket.",
  "main": "simple-test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "ws": "^8.18.3"
  }
}
